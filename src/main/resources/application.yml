spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/COWING?characterEncoding=UTF-8&serverTimezone=UTC
    username: howon
    password: 1230
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

logging:
  level:
    org.hibernate:
      type.descriptor.sql: trace #show parameter binding
      SQL: DEBUG

server:
  port: 8080